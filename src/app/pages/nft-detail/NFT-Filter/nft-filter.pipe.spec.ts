import { NftFilterPipe } from './nft-filter.pipe';

describe('NftFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NftFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
